{% extends 'base.html.twig' %}

{% block title %}Exercise{% endblock %}

{% block body %}
    <h1>Exercise</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exercise.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ exercise.name }}</td>
            </tr>
            <tr>
                <th>Chapter</th>
                <td>{{ exercise.chapter }}</td>
            </tr>
            <tr>
                <th>Keywords</th>
                <td>{{ exercise.keywords }}</td>
            </tr>
            <tr>
                <th>Difficulty</th>
                <td>{{ exercise.difficulty }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ exercise.duration }}</td>
            </tr>
            <tr>
                <th>OriginName</th>
                <td>{{ exercise.originName }}</td>
            </tr>
            <tr>
                <th>OriginInformation</th>
                <td>{{ exercise.originInformation }}</td>
            </tr>
            <tr>
                <th>ProposedByType</th>
                <td>{{ exercise.proposedByType }}</td>
            </tr>
            <tr>
                <th>ProposedByFirstName</th>
                <td>{{ exercise.proposedByFirstName }}</td>
            </tr>
            <tr>
                <th>ProposedByLasName</th>
                <td>{{ exercise.proposedByLasName }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ exercise.createdAt ? exercise.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_administration_exercise_index') }}">back to list</a>

    <a href="{{ path('app_administration_exercise_edit', {'id': exercise.id}) }}">edit</a>

    {{ include('administration/exercise/_delete_form.html.twig') }}
{% endblock %}
