<form method="post" action="{{ path('app_administration_classroom_delete', {'id': classroom.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classroom.id) }}">
    <button class="btn">Delete</button>
</form>
