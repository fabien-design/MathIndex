{% extends 'base.html.twig' %}

{% block title %}Classroom{% endblock %}

{% block body %}
    <h1>Classroom</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classroom.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ classroom.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_administration_classroom_index') }}">back to list</a>

    <a href="{{ path('app_administration_classroom_edit', {'id': classroom.id}) }}">edit</a>

    {{ include('administration/classroom/_delete_form.html.twig') }}
{% endblock %}
