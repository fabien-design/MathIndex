<form method="post" action="{{ path('app_administration_recommandation_delete', {'id': recommandation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recommandation.id) }}">
    <button class="btn">Delete</button>
</form>
