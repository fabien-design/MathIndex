{% extends 'base.html.twig' %}

{% block title %}Recommandation{% endblock %}

{% block body %}
    <h1>Recommandation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recommandation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ recommandation.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_administration_recommandation_index') }}">back to list</a>

    <a href="{{ path('app_administration_recommandation_edit', {'id': recommandation.id}) }}">edit</a>

    {{ include('administration/recommandation/_delete_form.html.twig') }}
{% endblock %}
