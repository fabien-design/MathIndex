{% extends 'base.html.twig' %}

{% block title %}MatIndex | Demande de réinitialisation{% endblock %}

{% block body %}
<h1 class="text-cse font-bold text-3xl pb-6">Demande de réinitialisation de mot de passe</h1>
<div class="bg-white rounded-lg p-8">
    <p class="pb-6">
        Cet espace est réservé aux demande de réinitialisation de mot de passe.
        Si vous n'avez pas encore de compte, veuillez effectuer votre demande
        directement en envoyant un email à <a href="mailto:contact@lyceestvincent.net?subject=Création d'un compte MathIndex" class="text-blue-600">contact@lyceestvincent.net</a>.
    </p>

    {{ form_errors(form) }}

    {# Début du formulaire #}
    {{ form_start(form, {'attr': {'class': 'max-w-sm'}}) }}

    <div class="mb-5">
        {{ form_label(form.firstname) }}
        {{ form_widget(form.firstname) }}
        {{ form_errors(form.firstname) }}
    </div>
    <div class="mb-5">
        {{ form_label(form.lastname) }}
        {{ form_widget(form.lastname) }}
        {{ form_errors(form.lastname) }}
    </div>

    {# Champ email #}
    <div class="mb-5">  
        {{ form_label(form.email) }}
        <div class="error-msg-container">
        {{ form_errors(form.email) }}
        </div>
        {{ form_widget(form.email) }}
    </div>

    <div class="flex gap-8">
        <a href="{{ path('app_login') }}" class="px-[2.5rem] rounded-lg py-5 bg-gray-100 text-neutral-600"><i class="fa-solid fa-chevron-left pr-5"></i>Retour à la page de connexion</a>
        <button class="btn px-12 rounded-lg py-5 bg-gray-100 text-neutral-600">Envoyer</button>
    </div>

    {# Fin du formulaire #}
    {{ form_end(form) }}
</div>

{% endblock %}
